[phases.setup]
nixPkgs = ["nodejs_20"]
cmds = ["npm install -g pnpm"]

[phases.install]
cmds = ["pnpm install"]

[phases.build]
cmds = ["pnpm --filter @workspace/dey-marketing run build"]

[start]
cmd = "npx serve -s artifacts/dey-marketing/dist -l $PORT"
